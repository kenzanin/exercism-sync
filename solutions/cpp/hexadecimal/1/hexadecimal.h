#include <cstdint>
#include <string>
#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H
#include "string"
namespace hexadecimal {
    std::uint32_t convert(std::string);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H