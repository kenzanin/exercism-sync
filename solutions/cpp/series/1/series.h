#include <vector>
#if !defined(SERIES_H)
#define SERIES_H

#include <string>
#include <vector>

namespace series {
std::vector<std::string> slice(const std::string &, int);
} // namespace series

#endif // SERIES_H