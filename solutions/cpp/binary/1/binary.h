#include <bits/stdint-uintn.h>
#include <cstdint>
#include <string>
#if !defined(BINARY_H)
#define BINARY_H
#include "string"
namespace binary {
    std::uint32_t convert(std::string);
}  // namespace binary

#endif // BINARY_H